 <!--NAV부분 입니다.. -->
    
    <nav class="navbar navbar-toggleable-md navbar-light bg-faded">
      <nav class="container mx-4">
             <button class="navbar-toggler navbar-toggler-right" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
          <a class="navbar-brand" href="https://crud-practice-donghan.c9users.io">Board</a>
          <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
              <li class="nav-item active">
                <a class="nav-link" href="/list/">list <span class="sr-only">(current)</span></a>
              </li>
            </ul>
          </div>
        </nav>
        </nav>
        

    
     
     <div class ="container mt-5">
             <div class="card">
          <div class="card-header">
            <%= @show_list.id %>
          </div>
          <div class="card-block">
            <h4 class="card-title"><%= @show_list.title %></h4>
            <p class="card-text"><%= @show_list.content %></p><br>
            <span style="font: italic bold;font-size: 10pt"><p class="card-text"><%= @show_list.editor %></p></span><br>
                <button type="button" class="btn btn-success mr-2"><a href="/edit/<%= @show_list.id %>">수정</a></button>
                <button type="button" class="btn btn-info"><a href="/delete/<%= @show_list.id %>">삭제</a></button>
          </div>
        </div>
        </div>
